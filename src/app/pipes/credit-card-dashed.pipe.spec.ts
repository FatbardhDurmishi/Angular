import { CreditCardDashedPipe } from './credit-card-dashed.pipe';

describe('CreditCardDashedPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditCardDashedPipe();
    expect(pipe).toBeTruthy();
  });
});
